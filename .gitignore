# Regenerated by the Bitburner filesync extension on connect
# (filesync.json -> definitionFile.update). Not source.
NetScriptDefinitions.d.ts

# SDD scratch workspace (ledger, briefs, review packages)
.superpowers/
